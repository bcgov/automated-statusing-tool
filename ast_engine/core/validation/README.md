## Validation

This module is stubbed out for developing the skeleton of the validation model
throughout the ast_engine; particularly the returned results objects and the Area of Interest.
Modules will use this module for it's base validation to be integrated into it's own specific
validation processes at runtime.
